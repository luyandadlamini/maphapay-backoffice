<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| API Routes for api.finaegis.org subdomain
|--------------------------------------------------------------------------
|
| These routes are loaded when the application is accessed via
| api.finaegis.org. They mirror the regular API routes but without
| the /api prefix.
|
*/

// When accessed via api.finaegis.org, routes don't need /api prefix
// The routes are loaded from the main api.php and api-v2.php files
// This file serves as documentation for the subdomain routing setup

// The actual routing configuration is handled in bootstrap/app.php
