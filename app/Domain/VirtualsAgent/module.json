{
    "$schema": "https://finaegis.io/schemas/module.json",
    "name": "finaegis/virtuals-agent",
    "version": "1.0.0",
    "description": "Virtuals Protocol — AI agent commerce via ACP with spending enforcement",
    "type": "feature",
    "dependencies": {
        "required": { "finaegis/shared": "^1.0" },
        "optional": { "finaegis/agent-protocol": "^1.0" }
    },
    "provides": {
        "interfaces": [],
        "events": ["VirtualsAgentRegistered", "VirtualsAgentActivated", "VirtualsAgentSuspended"],
        "commands": []
    },
    "paths": { "routes": "Routes/api.php", "migrations": null, "config": null, "tests": null }
}
